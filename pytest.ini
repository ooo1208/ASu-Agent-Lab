[pytest]
testpaths =
    tests
norecursedirs =
    src/download
addopts = -m "not integration and not live"
markers =
    integration: test requires live Web, MCP, Java ERP, or other external services
    live: test requires a live model-backed Agent service
