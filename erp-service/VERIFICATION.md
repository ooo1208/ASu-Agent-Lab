# ERP 验证记录

验证日期：2026-09-08。运行环境：Windows、Eclipse Temurin JDK 21.0.10、Maven 3.9.11、Spring Boot 3.5.16。

## 自动化验证

执行 `mvn -B -ntp package`，结果：**BUILD SUCCESS；12 tests，0 failures，0 errors，0 skipped**。可执行 Spring Boot JAR 成功生成。

覆盖内容：

- 订单头部分更新保留明细、编号、时间；显式明细替换重新计算金额；拒绝空明细后原记录不变。
- `0.10 × 3 = 0.30` 的精确金额、总额不匹配回滚、订单状态流转和终态保护。
- 4 个并发相同幂等键请求只创建一张订单；同键不同请求返回 409。
- 库存按关联零件名称筛选后分页；超额出库回滚；同键重复入库只增加一次。
- 两个并发出库竞争只允许一个成功，另一个返回 409，库存不为负数。
- 8 个 MCP 工具对应的数据结构和关联供应商/零件信息。
- 供应商、零件、订单、客户、物流和统计接口的业务 CRUD 流程；54 条目标 HTTP 路由已注册。
- 8 张关系业务表的关键金额列、用户外键、供应商外键、库存非负约束和明细金额 CHECK。
- 零件主数据的供应商改变后，订单头更新仍保留明细原供应商。
- API Key 缺失/错误拒绝、正确密钥通过，以及健康检查可访问。

自动化代码在 `src/test/java/dev/asu/erp/`。本记录的测试数量描述本次结果，不表示所有输入组合或全部企业采购业务已经覆盖。

## 独立进程与持久化

使用打包后的 JAR 在本机 18080 端口启动，以独立 H2 文件库进行真实 HTTP 验证：

1. 健康检查返回 `UP`；零件分页返回 3 条合成种子。
2. 创建 `HTTP-PERSISTENCE-001`，ID 为 2，金额 0.30。
3. 结束该 Java 进程，再启动同一个文件库。
4. 使用原 `Idempotency-Key` 重放创建请求，仍返回 ID 2；按订单号查询只有 1 张订单。
5. 通过 HTTP 只修改备注后，明细仍有 1 条，总金额仍为 0.30。

验收进程已停止，文件库和日志位于被 Git 忽略的 `target/`，未加入公开源码。

## 未验证范围

未连接真实 MySQL，未运行 Docker 镜像，也未接入真实企业数据、物流、付款或模型服务。MySQL profile 和 Dockerfile 已提供，但 H2 结果不能直接当作这些环境的通过证明。此模块以本地教学与业务工具联调为目标，吞吐、权限分级及大型数据集查询需要另行验收。
